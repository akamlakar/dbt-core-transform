version: 2

models:
  - name: dim_category
    description: "Dimension model for Amazon product categories."
    columns:
      - name: category_id
        description: "Unique identifier for the product category."
        tests:
          - unique
          - not_null

      - name: category_name
        description: "Name of the product category."
        tests:
          - not_null

